/**
 * 图片插件元数据
 */
import type { PluginMeta } from '../index'

export const manifest: PluginMeta = {
    kind: 'image',
    label: '图片',
    icon: '🖼️',
    description: '图片内容',
    editable: false,
    supportsCardMode: false
}
