# 开发环境配置

# API 基础 URL（留空则使用当前 host）
# VITE_API_URL=http://localhost:3000/api/v1

# WebSocket URL（留空则自动使用当前访问的 host）
# 本地开发单机测试时可设置为 localhost:3000
# 局域网多设备测试时请注释掉，让它自动适配
# VITE_WS_URL=localhost:3000
